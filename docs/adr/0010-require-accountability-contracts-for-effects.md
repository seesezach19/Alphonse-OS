---
status: accepted
---

# Require accountability contracts for effects

Every effectful capability must declare a valid Accountability Contract before activation so Kernel can determine whether delegated work completed, failed, stalled, lacks evidence, or requires recovery. Butler can therefore supervise arbitrary packages from shared outcome semantics rather than package-specific monitoring code.
