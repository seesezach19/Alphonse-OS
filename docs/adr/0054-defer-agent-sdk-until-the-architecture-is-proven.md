---
status: accepted
---

# Defer Agent SDK until the architecture is proven

Alphonse retains the optional protocol-derived Agent SDK as a long-term product surface but does not implement it before the n8n repair loop validates Workflow, revision, event, case, reproduction, worker, verification, and repair-delivery contracts. The SDK is generated from stabilized protocols during the subsequent OTLP and second-runtime proof.
