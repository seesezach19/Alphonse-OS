---
status: accepted
---

# Use customer-controlled Kernel Environments

Each customer operates an isolated Kernel Environment as its own authority domain, deployable locally, in customer cloud, or through managed hosting with identical semantics. Packages may be promoted across environments, but identity, delegation, activation, run, evidence, and recovery state remain environment-local so hosted coordination cannot become hidden business authority.
